## First thing that we will have to do is add the Ansible Infra code to a SCM repository. (Playbook / Roles). Create a new repository on GitHub and PUSH the Ansible code to the repo.

## Login to the Tower Automation platform
## Let's browse through the platform.
## On the left hand side we have,

1) View Section
2) Resource Section:
	a) templates : check the default template
	b) credentials: check default credentials for machine
	c) projects: check the default project
	d) Inventory: check the default inventory
	e) Inventory Scripts
3) Access Section
	a) organizations: check the default organization
	b) users: default admin user
	c) teams
4) administration section

